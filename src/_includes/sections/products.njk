<!-- products -->
<section class="section" id="kategorien">
  <div class="section-label">Meine Kollektionen</div>
  <h2 class="section-title">Alles für <em>kleine Entdecker</em></h2>
  <p class="section-sub">Von zarten Stramplern bis zu liebevollen Geschenksets – bei rakuhn findest du das Richtige für jeden Anlass.</p>

  <div class="gifts-grid">

  {% for product in collections.products %}
    <div class="gift-card" >
      <img class="gift-img" src="{{ product.data.icon }}" alt="{{ product.data.name }}" style="object-position: {{ product.data.iconPosition | default('center') }}" onclick="window.location.href='/products/{{ product.fileSlug }}/';" loading="lazy">
      <div class="gift-info">
        <div class="gift-name">{{ product.data.name }}</div>
        <div class="gift-price">{{ product.data.price }}</div>
        <button class="gift-btn gift-btn2" onclick="window.location.href='/products/{{ product.fileSlug }}/';">Stöbern</button>
        <button class="gift-btn" onclick="onClickProduct('{{ product.data.name }}', '{{ product.data.price }}');">Produkt anfragen</button>
      </div>
    </div>
  {% endfor %}

  </div>
</section>